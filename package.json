{
  "name": "waraide",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix waraide-app",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm start --prefix backend\" \"npm run dev --prefix waraide-app\"",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix waraide-app",
    "stop": "lsof -ti:3001,5173,5174 | xargs kill -9 2>/dev/null || true",
    "verify": "npm test --prefix backend",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
